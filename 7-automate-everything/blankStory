import type { Meta, StoryObj } from '@storybook/react'
import { replaceMe } from './replaceMe'

const meta = {
    title: 'replaceMe',
    component: replaceMe,
    parameters: {
        layout: 'centered'
    },
    tags: ['autodocs']
} satisfies Meta<typeof replaceMe>

export default meta
type Story = StoryObj<typeof meta>

export const Primary: Story = {
    args: {}
}
